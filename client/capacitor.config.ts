import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AprendiendoIonic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
